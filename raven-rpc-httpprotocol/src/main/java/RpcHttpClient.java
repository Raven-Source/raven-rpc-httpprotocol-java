import java.util.Map;

public interface RpcHttpClient {
    //<TData, TResult> TResult Invoke(String relativeUrl, TData contentData, Map<String , String> urlParameters, HttpMethod httpMethod, int timeout);

}
